@model IEnumerable<ProjektKino.Models.Reservation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.placesCount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.placesDict)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.clientId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.paid)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.placesCount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.placesDict)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.clientId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.paid)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.seanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.seanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.seanceId })
        </td>
    </tr>
}

</table>
